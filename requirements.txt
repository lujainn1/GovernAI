fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.6
openai>=1.30
PyYAML>=6.0
python-dotenv>=1.0
httpx>=0.27
pytest>=8.0
python-multipart>=0.0.9
pypdf>=4.2
python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1
# Optional: OCR fallback for scanned PDFs in the Document Processing Agent
# (app.tools.ocr). Also requires the tesseract-ocr and poppler system
# binaries (with the Arabic tesseract language pack) to actually run OCR -
# without them, extraction still works, just without the OCR boost.
pytesseract>=0.3.10
pdf2image>=1.17
